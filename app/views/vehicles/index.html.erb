<h2> veiculos </h2>

<% @vehicles.each do |vehicle| %>
    <p>
        <%= vehicle.id %>
         <%= vehicle.brand %>
         <%= vehicle.model %>
         <%= vehicle.year %>
         <%= vehicle.kind %>
    </p>

<% end %>

